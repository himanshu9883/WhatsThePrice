<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.techrums.whatstheprice">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:name=".TutorialApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Launcher.child">
        <activity android:name=".ui.activities.CommentActivity"></activity>
        <activity android:name=".ui.activities.CustomRegisterActivity" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".ui.activities.AboutUsActivity" />
        <activity
            android:name=".MainActivity"
            android:label="Whats the Price">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.LoginActivity"
            android:label="Sign In" />
        <activity
            android:name=".ui.activities.RegisterActivity"
            android:label="Sign Up" />
        <activity
            android:name=".ui.activities.HomeActivity" />
        <activity
            android:name=".ui.activities.EditProfileActivity"
            android:label="Edit Profile" />
        <activity
            android:name=".ProfileActivity"
            android:label="Profile" />
        <activity android:name=".ui.activities.PostActivity" />
    </application>

</manifest>